[project]
name = "django-install-configure"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["my_site", "app_test", "my_site_2"]
